# Mobile

Expo app with minimal flows:
- Email auth
- Subscription via Stripe Checkout (web)
- Generate documents (upload resume + job URL)
- History + mark applied

Set `EXPO_PUBLIC_API_URL` in app config or use Metro env.


